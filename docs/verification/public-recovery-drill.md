# 公开版本的隔离恢复演练

2026-09-13，基于生产实现 3a54ab0 和刷新运行 [34735383243](https://github.com/imjszhang/aipoch-network/actions/runs/34735383243) 的白名单公开观察，执行真实 `catalog:recover` CLI 与完整 `buildRelease`。生产网站未改动。机器可读结果见 [报告](public-recovery-drill.json)。

在独立 `.cache/recovery-live-drill/` 中复制生产候选；当前注册目录仅在演练副本中注入错误标题，并将 scikit-learn 资源标记为模拟撤回。这里不代表上游实际撤回或不可访问。

验证结果：

1. 注入构建失败后，原生产候选的完整文件树摘要保持 `1993cc78004d670f9604d4d1a775f668a43944b192d9031cf87b6a45c1065bee`。
2. 恢复 CLI 使用旧编辑内容和当前观察，恢复 SciPy 标题，原样保留当前撤回列表。
3. 完整恢复构建通过 93 页静态校验，模拟撤回资源未进入内部目录，错误标题消失；新候选 snapshot 为 `44586393fc680644b7b0f7c8`。
4. 本次恢复没有重新部署旧 ZIP，也没有执行生产故障注入。结合先前线上发布前后 40 文件不变记录，分别证明本地故障保护和正常生产切换，不能把二者写成生产故障回滚已执行。

首轮演练脚本误把内部 JSON 包装对象当作 catalog 读取，断言代码报错；修正为 `.catalog` 后，重新从干净生产候选开始，全部断言通过。该问题位于临时演练脚本，未修改产品实现。
