# Issue #5 修复验收账本

日期：2026-09-14。实施依据：[局部设计修订与完整计划](../../../design/changes/issue-5-connection-guidance.md)，问题：[Issue #5](https://github.com/imjszhang/aipoch-network/issues/5)。本页负责记录实际证据；待填项表示尚未证明，不按计划或已有历史测试推定通过。

本地修复与本轮授权的验收已完成：类型检查、295 项离线测试、334 项浏览器检查通过，56 项按模式不适用跳过，0 失败／0 flaky。六种 mode/base 候选和 50 张合成截图已归档身份；**H01 由用户反馈完成，H02–H04 经授权的产品 UI 辅助操作并由用户亲自批准后通过**。本地 PR 材料已准备；远端 PR、生产发布、设计团队正式签认及 Issue 关闭分别尚未执行，不作为本地实施验收的未完成项。可审计的测试名称、报告摘要、源码输入、六份产物清单及原件摘要见 [validation.json](validation.json)。

## 当前状态与身份

| 项目 | 当前事实 |
| --- | --- |
| 用户授权 | 实施计划并验收；设计团队正式签认、发布各自记账 |
| 开始提交 | `72419cdf51d56edd9d182b812218ef5aad7af2aa` |
| 最终基线 | 工作中快进合并 `f52a50c8640089797cd14555421c26f39b268f6e`，保留已合入的 MuData 与中文指南；最终检查使用 21 sources / 21 projects / 22 resources |
| 原设计 | v9-r2 HTML + MD 修订 1，五份文件于 2026-09-14 重新核对 SHA-256，均与接收记录一致 |
| Connector 能力 | 已核实公开 MCP 查看／打开确认页路径；固定来源提交 `74c066904326d2d5a21e5c9d949d31e20238c20c`，证据见设计修订第 2 节 |
| 本地实现 | `e1d530ac4fcacabacbb714f936b49a5ac9ffaf90`；证据文档另行提交，不冒称远端已合并 |
| 自动化与视觉 | 295 项离线、334 项相关浏览器检查通过；50 张合成截图、35 个状态测量完成，根任务已阅读关键原稿／实际页面对照，详见下文 |
| 真实四场景 | 本轮授权范围内完成：H01 为用户按提示完成的明确反馈；H02–H04 为产品 UI 辅助操作、用户亲自批准后的实际观察，角色范围见下文 |
| 设计团队正式签认 | 待记录，不妨碍已授权的实施和独立验证 |
| PR／提交／合并 | 本地代码已提交；[PR 文案草稿](pull-request.md)已准备，尚未提交远端 PR／合并 |
| 发布／线上复核／Issue 关闭 | 尚未执行；本地验收不表示已经上线 |

## 完成条件审计

| ID | 必须证明的结果 | 证据形式 | 当前结论 |
| --- | --- | --- | --- |
| A01 | 用户知道码用于两端比对，无需输入网页；普通用户打开路径准确 | 当前文案、复制文本和 H01/H02 观察 | 文案与复制内容检查通过；H01 有用户按提示完成的明确反馈，H02 验证页面路径可由产品 UI 辅助操作完成，不扩大为独立用户可用性证明 |
| A02 | 连接成功保留成功状态，不自动跳入审阅或重开已关闭面板 | 状态测试、浏览器流程、H02 | 实现及合成浏览器检查通过；真实 H02 用户批准后仍停留连接面板，无自动审阅 |
| A03 | 明确继续恢复同一对象／当前准确引用，重新确认前不能发送 | 状态与浏览器测试、H02 | 实现及状态／浏览器检查通过；真实 H02 显式继续才打开同一 AnnData 引用，确认未选、发送禁用且无新回执 |
| A04 | 取消、到期、重试、迟到响应、重复点击均不错误推进 | 状态／协议／浏览器测试、H03 | 截止／取消／新尝试／迟到守卫通过；真实 H03 自然到期、显式换码重试和用户批准后连接通过，未冒称真实旧请求迟到批准测试 |
| A05 | 确认页未打开、工具未发现、复制失败均有准确恢复路径 | 浏览器失败分支、H04 | 复制失败、初始配对 403 和到期后帮助通过；真实 H04 依帮助新建会话恢复、用户亲自批准后通过，属于授权辅助操作范围 |
| A06 | unavailable、Demo、real 与静态根／子路径正常，公共浏览和个人守卫保留 | 类型／离线检查与六种产物浏览器矩阵 | 当前候选相关路径通过；实际覆盖与跳过原因见下表，不宣称全部浏览器兼容 |
| A07 | 关键状态、1440/1024/390/360、键盘／焦点／200% 与减少动效可用 | 真实渲染截图和运行时观察 | 本次截图、键盘、触控模拟、减少动效及真实 200% 范围通过；35 个状态无横向溢出，独立阅读覆盖五种显示条件；完整读屏未测 |
| A08 | 设计差异、实施映射和 R/V 回链完整，原件不变 | 文档链接检查、原件摘要检查、最终差异审计 | 记录与回链已建立，五份原件摘要不变；设计团队正式签认另待记录 |
| A09 | 四个真实场景逐项记录操作角色和证据范围，批准仍由用户决定；独立用户可用性与授权辅助操作分开 | 下方 H01–H04 逐项记录及后续角色授权 | 本轮授权范围内通过：H01 为用户明确反馈，H02–H04 为产品 UI 辅助操作、用户亲自批准 |
| A10 | 提交／PR 材料准确说明变化、验证及限制；发布状态单独记录 | 最终源码身份、PR／交付材料、发布记录（若获授权执行） | 本地交付材料完成：代码提交与 PR 文案已准备；本页与 validation 随证据文档提交记录，以文件 Git 历史定位，避免自引用提交摘要；远端 PR／发布未执行 |

## 自动化执行记录

使用 `.node-version` 的 Node `24.18.1`、现有 lockfile、Playwright `1.63.0` 和 Chromium `153.0.8010.12`。最终构建均显式设置 `SOURCE_BATCH=fixtures/pilot/snapshots.json`；工作中 main 新增 MuData 后，既有 `.cache/batch.json` 仍只有 20 个来源，默认构建正确拒绝不完整批次。保留缓存原文并改用已提交的 21 来源离线输入，未刷新上游，也未把缺少来源解释为撤回。最终快照为 `2c239307f62698d0f5496274`。

根路径使用 `dist`、`dist-demo`、`dist-real`；子路径使用隔离的 `/tmp/aipoch-issue5-sub-{unavailable,demo,real}`。构建逐个执行，避免共享目录生成锁竞争。所有最终静态服务设置 `CI=true`，拒绝复用已有服务。以下数量只来自最新输入和最终修复，早期结果不累加。

| 检查 | 预期执行范围 | 实际结果／证据 |
| --- | --- | --- |
| 类型检查 | `npm run typecheck` | exit 0；输出摘要见 validation.json |
| 离线测试 | `npm test`；含状态、适配器、目录、独立消费及模式边界 | 295 通过，0 失败／跳过；其中工作台状态测试 34 项 |
| unavailable 根路径 | 99 页／132 文件；site、community、directory-navigation、v9-workbench | 56 通过，24 项 Demo 场景跳过；4221／4218 端口 |
| Demo 根路径 | 100 页／133 文件；v9-workbench | 28 通过，4 项 unavailable 场景跳过；4217 端口 |
| real 根路径 | 99 页／132 文件；real-connector | 26 通过，0 跳过；4216 端口 |
| unavailable 子路径 | 99 页／132 文件；site、community、directory-navigation、navigation-state、v9-workbench | 62 通过，24 项 Demo 场景跳过；4223 端口 |
| Demo 子路径 | 100 页／133 文件；同上 | 82 通过，4 项 unavailable 场景跳过；4224 端口 |
| real 子路径 | 99 页／132 文件；前四项公共场景与 real-connector | 80 通过，0 跳过；4225 端口 |
| 原件与文档 | 五份原件摘要、链接、源码／产物身份和公开证据检查 | 五份原件通过；当前文件清单及摘要已归档，最终文档审阅另记 |

最终浏览器运行开始时间为 2026-09-14 09:05:17–09:05:29 UTC，最晚一组约于 09:06:04 UTC 完成。334 通过／56 跳过／0 失败／0 flaky；根路径和子路径的文件集合有意分别列明，不能把其中一组描述为全仓库完整浏览器套件。完整测试名、桌面／移动结果、执行端口、每组报告 SHA-256 和对应产物详见 [validation.json](validation.json)。

可复现构建形式为 `SOURCE_BATCH=fixtures/pilot/snapshots.json VITE_WORKBENCH_MODE=<mode> SITE_BASE=<base> npm run build -- --output <directory>`；浏览器使用对应 `TEST_MODE`、`TEST_BASE`、`TEST_OUTPUT`、`TEST_PORT`、独立报告／结果目录及 `npm run test:e2e -- <上表文件>`。真实协议测试使用合成路由应答，未接触真实用户批准。

关键回归需要核对**覆盖内容**，不能只记录绿色总数：无选择／有选择；关闭等待面板后成功；取消后迟到批准；到期重试与旧码；连接中变更选择；旧会话与目录变化；完整手工引用；剪贴板拒绝；后台恢复后倒计时；未连接个人入口隐藏和处理器守卫；断开保留记录；刷新不恢复连接；Demo 隔离及错配回执不成功。发送回归在合成环境中执行，不自动触发真实引用发送。

本次确实发现并修复了两个中间问题：异步移除配对控件后的焦点恢复最初先于打开控件记录，导致关闭时无法回到原按钮；调整效果执行顺序后，最终原焦点和异步焦点用例均通过。另将帮助从只存在于等待区改成在 real 模式初始失败／到期／中断后继续可达，新增初始 POST 403 和到期帮助检查。早期失败及旧目录绿色结果仅作修复历史，不算最终通过。

## 视觉和可操作性证据

当前 [visual-results.json](visual-results.json)由 `scripts/verify-connection-guidance.mjs` 在 2026-09-14 09:07:25 UTC 开始的最终采样生成，绑定源码摘要、Chromium `153.0.8010.12` 与 real 构建。50 张图均使用合成连接响应或原始设计 Demo；35 个实际状态测量覆盖五组显示条件。原稿与当前实现分开命名，不冒充历史生产截图或真实批准。

| 状态 | 对照要求 | 当前证据 |
| --- | --- | --- |
| 未连接、无对象 | 原稿与新候选同视口：唯一入口、Connect/Get、无个人操作 | [桌面原稿](screenshots/desktop-reference-disconnected.png)、[桌面实现](screenshots/desktop-disconnected.png) |
| 等待确认、有对象 | 码／倒计时／三步／复制／帮助与待接续分区 | [桌面等待](screenshots/desktop-waiting.png)、[390px 等待](screenshots/mobile-waiting.png)、[360px 下方操作](screenshots/narrow-waiting-actions.png) |
| Connected、有对象 | 同视口原稿／实现；明确成功、可关闭、具名继续动作，无自动审阅 | [桌面原稿](screenshots/desktop-reference-connected.png)、[桌面实现](screenshots/desktop-connected.png)、[360px 实现](screenshots/narrow-connected.png) |
| 过期／未打开帮助 | 原因、重试、浏览和帮助继续可达 | [桌面帮助](screenshots/desktop-help.png)、[360px 到期](screenshots/narrow-expired.png)、[200% 到期](screenshots/zoom200-expired.png) |
| 完整引用审阅 | 真实目录来源、版本、许可，未确认发送禁用 | [桌面审阅](screenshots/desktop-review.png)、[200% 审阅](screenshots/zoom200-review.png) |

全部文件遵循 `screenshots/{desktop,tablet,mobile,narrow,zoom200}-{disconnected,selected,waiting,help,connected,review,expired,waiting-actions,reference-disconnected,reference-connected}.png`。文件清单和单图摘要在 validation.json。原型没有真实比对码阶段，因此等待引导使用本次局部修订作依据，不伪造对应原型确认入口。

35 个测量状态中，文档 `scrollWidth` 均不大于实际 `innerWidth`，对话框没有水平溢出。200% 使用浏览器 tabs zoom API 并读取 `getZoom=2`，实际 `outerWidth=1440`、`innerWidth=720`、`devicePixelRatio=2`，不是 CSS 放大。等待长面板采用内部滚动；下方取消和保留对象区通过焦点操作进入可见范围。采样检查了 Tab 焦点约束、手机尺寸 tap 输入、减少动效和零引用发送；这些是 Chromium 自动化／触控模拟，不是物理手机声明。

独立复核代理阅读了五种显示条件下的 26 张实际截图，根任务另阅读关键原稿和实际截图，包括 360px、真实 200%、等待下方操作、到期帮助和连接成功分区：保留 520px 浮窗体系，未见非预期重叠、横向裁切或超出预期内部滚动的操作截断。源码／视觉摘要再次核对一致。截图的测量／阅读与设计团队正式签认仍是两件事；完整旁白验收未做，不记通过。

至少核对 1440、1024、390、360 CSS 像素；记录真正浏览器 200% 的设置／测量，不能用字号、transform 或窄视口代替。确认长来源／对象名没有横向溢出；等待面板有可用滚动与关闭操作。检查 Tab/Shift+Tab、Esc、进入和返回焦点，关闭后迟到成功不抢焦点，触控模拟与减少动效。倒计时不逐秒强制播报；真实读屏未做则如实写未测，沿用用户先前停止旁白专项的决定。

历史参考仍可定位到 [v9-r2 工作台复核](../v9-r2/workbench-review.md#截图索引)，其旧结果不覆盖本次修改；本次链接、测量和源码／产物身份使用以上独立材料。

## 真实验收：四个场景与操作角色

在开始前记录日期、Chrome／macOS／Open-Science／Connector 实际版本，候选地址的 origin、base、构建模式、源码／产物摘要，浏览器本机网络访问权限和 Connector 已设置状态。先使用 Issue 的 macOS Chrome 环境；其他平台没有测试则不宣称兼容。只依据公开工具／产品 UI，不读取私有配置或把凭据写入证据。

本次实际环境为 macOS `26.6.2`、原生 Chrome `152.0.7977.84`、Open-Science `0.29.0`、Connector `0.1.0-alpha.3`；候选为 `http://127.0.0.1:4186` 的 real 根路径。首次尝试未进入可完成配对：该 origin 的只读 OPTIONS 为 403 且没有允许来源头；生产 `https://aipoch.network` 返回 204。用户随后明确授权通过公开 `setup --origin http://127.0.0.1:4186` 追加开发来源，结果 `configurationApplied: true`，之后开发与生产来源 OPTIONS 均为 204，宿主 ready。该操作保留生产来源，会使旧内存配对／会话失效，不构成用户配对批准。

设置后，原生 Chrome 已观察到无保留对象的真实等待引导。用户随后明确反馈：“已按提示批准，网页显示 Connected”。据此 H01 记录为用户亲自完成并报告成功，未由代理批准；Connected 状态依据用户反馈，不宣称另有原生界面复核。首次失败仍保留，来源许可／hostReady 本身不作为成功依据。

这次成功反馈对应用户已打开的候选页面，其目录可能仍为合并 main 前加载的 20 来源状态；成功路径的引导和显式连接语义与最终源码一致，但不将此反馈写成最终 21 来源产物的全面原生验收。剩余用例在刷新后的当前候选继续。此流程按要求未发送引用；没有记录真实短码、请求票据、确认私有地址或私人研究内容。

后续角色授权：用户明确回复“我已经断开了，你帮我操作吧”，授权代理通过产品 UI 辅助 H02–H04，真实比对和批准仍由用户亲自完成。H01 已有独立用户反馈保持原记录；后续辅助操作只证明相应功能与页面路径，不冒称独立用户可用性，也不因此要求用户重新独立执行全部步骤。四个用例均无需发送引用。

H02 于 2026-09-14 约 09:19–09:22 UTC 完成：使用可见 Codex in-app browser 的当前 21 来源 real 候选及原生 Open-Science `0.29.0`。代理从 Network 页面为 AnnData 发起连接、复制请求并在 Open-Science 聊天发送。旧会话报 `Conversation graph materialization failed`；通过产品“新建”会话发送同一请求后正常打开确认页，用户明确回复“已批准”。代理随后读取实际界面：连接面板显示 `Open-Science connected`，AnnData 保留且未自动审阅；点击 `Review reference for AnnData research software` 后才进入完整审阅，对象为 `resource:anndata-library`，快照为 `2c239307f62698d0f5496274`，复选框未选、`Send reference` 禁用；关闭后页面仍为 0 receipts。此证据属于用户授权辅助操作和用户亲自批准，不属于原生 Chrome 或独立用户完成。旧会话错误保留为恢复过程记录。

H03/H04 过程记录：约 09:21:39 UTC 通过产品 UI 断开并发起新的真实请求、展开帮助，未复制或发送请求到 Open-Science，也未批准。等待真实三分钟后，09:24:44 UTC 实际界面显示 `Not confirmed` 及配对到期、可重试或继续浏览的说明；AnnData 保留，手工引用、Connect、Continue browsing 和帮助仍可达，没有自动重试。再次展开帮助后显式 Connect，新码与前次不同、计时恢复三分钟、AnnData 保留。约 09:25 UTC 按帮助新建 Open-Science 会话并发送页面生成的新请求。本次没有尝试批准旧请求，不把该观察写成真实迟到批准竞争验证；旧响应竞争由已归档自动化覆盖。

H03/H04 最终结果：约 09:26:20–09:26:36 UTC，原生 Open-Science 新会话明确报告已为新请求打开本机确认页，且未代为批准。根任务随后读取 IAB 实际页面为 Connected，仍停留连接面板并保留 AnnData；用户明确确认“是，刚才已亲自批准”。据此两场景在产品 UI 辅助操作、用户亲自批准的授权范围内通过，无临时 CLI 补步骤、无引用发送。没有捕获确认页打开后、用户批准前的短暂界面，因此不声称直接观察该瞬间；打开页面与批准的分离由 Open-Science 报告、用户反馈和已有自动化守卫共同支持。收尾关闭连接面板后，IAB 首页保持 Connected，Selected research 为 AnnData，页面明确显示 `0 reference receipts in this session`。

| ID | 起点与操作步骤 | 必须观察的结果 | 实际记录 |
| --- | --- | --- | --- |
| H01 无保留对象 | 使用没有本次选择的浏览器上下文；正常点击 Open-Science → Connect；按页面步骤在 Open-Science 中请求查看准确来源／代码的待确认连接；用户在本机页核对并批准；返回网站 | 不靠测试人员补步骤；代码用途清楚；网站验证后显示 Connected；可停在成功、关闭或回研究首页；无审阅弹出或新回执 | 通过：首次 origin 403 经用户授权设置解决；随后用户明确反馈已按提示亲自批准且网页 Connected。属于用户报告的真实路径完成，观察与载入候选范围见上文 |
| H02 保留 AnnData | 未连接时从能力目录／详情选择 AnnData；连接时核对保留区；按同样步骤批准；先停留成功，再点击具名 Review reference 动作 | 等待时无含糊 Continue；成功不自动跳转；原目录／筛选／对象保持；主动继续才进 AnnData 完整审阅；Send reference 未确认时禁用；不发送 | 通过：代理按产品 UI 辅助，用户亲自批准；连接后保持成功面板，显式继续进入同一 AnnData 当前快照审阅，发送禁用，0 receipts；详见上文环境与范围 |
| H03 到期后重试 | 发起连接但不批准，观察真实截止后到期；依提示明确重试；检查旧码不可推进；用户按新请求步骤完成批准 | 到期时旧请求不使网页 Connected；新尝试的码／截止更新，保留对象；不自动重试；重试后按页面路径完成，逐项记录辅助角色 | 通过：真实到期后仍未连接，显式重试产生不同码并重置时限，AnnData 保留；用户确认亲自批准新请求后实际页面 Connected。旧请求迟到批准竞争仅由自动化覆盖 |
| H04 未打开确认页 | 发起连接，先不打开本机确认页，展开 “Can’t find the confirmation page?”；按帮助进入新 Open-Science 会话／设置帮助并请求打开准确待确认页；若到期则按提示重试；用户亲自批准 | 帮助能恢复页面路径；打开页本身不显示 Connected；用户批准后才成功；若仍需产品流程之外的临时 CLI 补救或工具根本不可用，记录失败／外部依赖，不记通过 | 通过：先不打开页，真实到期后依帮助明确重试、进入新会话恢复，用户亲自批准；无临时 CLI 补步骤。属于辅助操作功能验收，批准前短暂界面未直接捕获，证据边界见上文 |

每个实际记录包含开始／结束时间、观察者角色、是否自行按提示完成、遇到的误解、页面最终状态、是否有临时人工补步骤和是否未发送引用。场景内失败保留原结果，修复后新增重验记录，不覆盖失败证据。

## 公开证据与隐私

公开截图只使用合成短码和合成会话、公开目录内容。不得归档真实私有确认 URL／ticket、poll/session/owner token、私人项目／文件／研究内容；真实短码即使不是授权凭据也不写入本次公开证据。不要公开原始网络 HAR、控制台或 CLI 全量输出；需要的协议结果以不含凭据的断言或手工摘要记录。真实验收只记录必要的环境、步骤和结果，不先把敏感截图提交再删除。

## 最终本地交付与另行记录的事项

当前源码身份、目录快照、每种 mode/base 的产物清单、逐项测试与截图已绑定 [validation.json](validation.json)。I5-1 能力核实、I5-2 设计记录、I5-3/I5-4 本地状态与页面、I5-5 不增协议的边界及 I5-6 自动化／视觉证据已完成；I5-7 的四场景按最新授权范围完成，H01 为用户完成反馈，H02–H04 为产品 UI 辅助操作及用户亲自批准；I5-8 的本地可审查 PR 材料已准备。本地实施和验收目标完成，远端 PR、发布、正式设计签认与 Issue 关闭各自另行记录，不作为本地剩余任务。

设计团队正式对齐意见单列日期与角色；没有意见时保留待对齐，不冒称签认。PR／发布应链接本页和设计修订，文案围绕最终问题、行为、验证与已知限制。生产部署及线上复核只在对应授权范围内执行；部署尚未执行时明确“本地候选”，不能把历史生产真实模式当成本次已上线。

若后续发布需要回退，目标为已验证的上一完整产物或经审查的 unavailable 产物，并保留适用公共目录和本机数据。后续新出现的能力缺口另行记录；本次完成范围和既有证据保持可追溯。
